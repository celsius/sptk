*** _zerodf.c.orig	Wed Mar  1 16:42:41 2000
--- _zerodf.c	Wed Mar  1 15:33:43 2000
***************
*** 4,10 ****
  
      All Zero Digital Filter
  
! 	double	zerodf(x, b, m, d, ngain)
  
  	double	x   : input
  	double	*b  : MA coefficients
--- 4,10 ----
  
      All Zero Digital Filter
  
! 	double	zerodf(x, b, m, d)
  
  	double	x   : input
  	double	*b  : MA coefficients
***************
*** 15,28 ****
  
  *****************************************************************/
  
! double	zerodf(x, b, m, d, ngain)
  double	x, *b, *d;
! int	m, ngain;
  {
      register int  i;
      double 	  out;
  
!     out = (ngain) ? x : b[0]*x;
  
      for(m--; m>0; m--){
  	out += b[m+1] * d[m];
--- 15,28 ----
  
  *****************************************************************/
  
! double	zerodf(x, b, m, d)
  double	x, *b, *d;
! int	m;
  {
      register int  i;
      double 	  out;
  
!     out = b[0]*x;
  
      for(m--; m>0; m--){
  	out += b[m+1] * d[m];
***************
*** 34,48 ****
      return(out);
  }
  
! double	zerodft(x, b, m, d, ngain)
  double	x, *b, *d;
! int	m, ngain;
  {
      register int        i;
      double              out;
  
!     out = (ngain) ? x : b[0]*x;
!     out += d[0];
  
      for(i=1; i<m; i++)
  	d[i-1] = b[i] * x + d[i];
--- 34,47 ----
      return(out);
  }
  
! double	zerodft(x, b, m, d)
  double	x, *b, *d;
! int	m;
  {
      register int        i;
      double              out;
  
!     out = b[0]*x + d[0];
  
      for(i=1; i<m; i++)
  	d[i-1] = b[i] * x + d[i];
